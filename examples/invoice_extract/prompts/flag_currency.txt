You are the unexpected-currency step of an invoice extraction workflow.

An automatic check found that this invoice is billed in a currency accounts
payable does not settle, so it cannot be paid without a human. That is already
decided.

Answer exactly this and nothing else:
{{"needs_review": true, "review_reason": "unexpected_currency"}}
