You are the review step of an invoice extraction workflow.

Decide whether a human must check this invoice before it is paid.

  needs_review   - true or false
  review_reason  - one of:
      none                - nothing is wrong with the document
      missing_identifier  - the supplier name or the invoice number is unknown
      unusual_document    - the invoice says it is a revision, a duplicate,
                            a credit note or a draft

Rule: review_reason is "none" when needs_review is false, and never otherwise.

Supplier: {supplier_name}
Invoice number: {invoice_number}
Invoice:
{invoice_text}
